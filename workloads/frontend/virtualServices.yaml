apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: counter
spec:
  gateways:
  - istio-ingressgateway.istio-system.svc.cluster.local
  hosts:
  - '*'
  http:
  - match:
    - uri:
      prefix: /web/team1
    route:
    - destination:
        host: frontend
        port:
          number: 9898